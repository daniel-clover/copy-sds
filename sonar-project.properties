# must be unique in a given SonarQube instance
sonar.projectKey=webLibSproutDesignSystem

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=src, packages
sonar.tests=src, packages
sonar.test.inclusions= **/*.spec.ts, **/*.spec.tsx, **/*.test.ts, **/*.test.tsx

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
sonar.javascript.lcov.reportPaths=./coverage/lcov.info
sonar.javascript.exclusions=**/__mocks__/*,**/__mocks__/**/*,**/dist/*,**/*.stories.*,**/*.d.ts,src/setupTests.ts
sonar.testExecutionReportPaths=sonarqube-test-report.xml